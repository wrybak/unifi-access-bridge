# Tests to implement

Suggested pytest coverage:
- config flow happy path
- port fallback
- unlock button
- open/closed updates
- HA camera proxy mode
- RTSP mode
- snapshot fallback mode
- mapping persistence
- websocket reconnect
